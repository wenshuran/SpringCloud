spring:
  cloud:
    bus:
      refresh:
        enabled: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: test
management:
  endpoints:
    web:
      exposure:
        include: '*'